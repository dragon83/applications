= simple_form_for @caste, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = f.association :religion
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), castes_path, :class => 'btn'
